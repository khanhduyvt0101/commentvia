{
	"buildCommand": "cd ../.. && bun install --frozen-lockfile && bun run build:website",
	"installCommand": "cd ../.. && bun install --frozen-lockfile",
	"outputDirectory": "dist",
	"framework": "vite"
}
